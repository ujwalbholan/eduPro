name: PR Checks

# Runs on every PR into develop or main. Add "Checks / checks" as a required
# status check in branch protection for both branches so nothing merges
# without passing this.
on:
  pull_request:
    branches: [develop, main]

jobs:
  checks:
    uses: ./.github/workflows/checks.yml